{
  "mcpServers": {
    "make": {
      "command": "npx", 
      "args": [
        "-y",
        "mcp-remote",
        "http://localhost:3001/sse",
        "--static-oauth-client-metadata",
        "{ \"scope\": \"openid profile email env:read org:write\" }"
      ]
    }
  }
}